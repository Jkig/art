export default function Post(props) {
    // take in an object, and send out a box with all the stuff.
    return (
        <div>
            <h1>props.title #pretend lol</h1>{/* need to probs pass this into img and overlay on img */}
            <img />{/* img source, need to ficure out how to pull from server */}
            {/* full post is probs a different function */}
        </div>
    )
}